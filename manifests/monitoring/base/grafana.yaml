apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  labels:
    dashboards: "grafana"
spec:
  config:
    security:
      admin_user: root
      admin_password: secret
  service:
    metadata:
      annotations:
        external-dns.alpha.kubernetes.io/hostname: grafana.k01.satoruh.org
    spec:
      type: LoadBalancer
